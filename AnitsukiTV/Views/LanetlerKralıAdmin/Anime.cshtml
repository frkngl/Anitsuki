@using AnitsukiTV.Models
@model PagedList.IPagedList<TBLANIME> 
@{
    ViewBag.Title = "Anime";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div style="position:relative;" class="card-body">
                <nav style="position:absolute; right:0;" class="navbar navbar-expand-lg bg-body-tertiary">
                    <div class="container-fluid">
                        <div class="collapse navbar-collapse" id="navbarSupportedContent">
                            <form action="@Url.Action("Anime", "LanetlerKralıAdmin")" class="d-flex" role="search">
                                <input class="form-control me-2" type="search" placeholder="Search" name="anime" aria-label="Search">
                                <button class="btn btn-outline-success" type="submit">Ara</button>
                            </form>
                        </div>
                    </div>
                </nav>
                <h4 class="card-title"><a class="btn btn-primary" href="/LanetlerKralıAdmin/AddAnime">Anime Ekle</a></h4>
                <p class="card-description">
                    Animeler
                </p>
                <table class="table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>RESIM</th>
                            <th>ANIME</th>
                            <th>TARIH</th>
                            <th>Güncelle</th>
                            <th>Durum</th>
                            <th>Sil</th>
                            <th>Slider</th>
                            <th>Detay</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var x in Model)
                        {
                            <tr>
                                <td>@x.ID</td>
                                <td><img style="width:100px; height:100px; object-fit:cover;" src="~/IMG/@x.BANNER" /></td>
                                <td>@x.ANIMENAME</td>
                                <td>@x.DATE.Value.ToString("dd.MM.yyyy")</td>
                                <td><a class="btn btn-warning" href="/LanetlerKralıAdmin/UpdateAnime/@x.ID">Güncelle</a></td>
                                <td>
                                    @if (x.STATUS == true)
                                    {
                                        <a href="/LanetlerKralıAdmin/ActivePassive1/@x.ID" class="btn btn-success btn-sm">Aktif</a>
                                    }
                                    else
                                    {
                                        <a href="/LanetlerKralıAdmin/ActivePassive1/@x.ID" class="btn btn-warning btn-sm">Pasif</a>
                                    }
                                </td>
                                <td>@Html.ActionLink("Sil", "Deletee", new { id = x.ID }, new { @class = "btn btn-danger btn-sm", onclick = "return confirm" + "('Bu Animeyi silmek istediğinize emin misiniz?');" })</td>
                                <td>
                                    @if (x.ADMINSTATUS == true)
                                    {
                                        <a href="/LanetlerKralıAdmin/ActivePassive2/@x.ID" class="btn btn-success btn-sm">Aktif</a>
                                    }
                                    else
                                    {
                                        <a href="/LanetlerKralıAdmin/ActivePassive2/@x.ID" class="btn btn-warning btn-sm">Pasif</a>
                                    }
                                </td>
                                <td><a style="color:#ffffff;" href="/LanetlerKralıAdmin/AnimeDetail/@x.ID" class="btn btn-light">Detay</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<div style="width:100%; display:flex; justify-content:center;">
    <nav aria-label="Page navigation example">
        <ul class="pagination">
            @if (ViewBag.CurrentPage > 1)
            {
                <li class="page-item">
                    <a class="page-link" href="@Url.Action("Anime", new { anime = ViewBag.Anime, p = ViewBag.CurrentPage - 1 })" aria-label="Previous">
                        <span aria-hidden="true">&laquo;</span>
                    </a>
                </li>
            }
            @for (int i = 1; i <= ViewBag.PageCount; i++)
            {
                <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                    <a class="page-link" href="@Url.Action("Anime", new { anime = ViewBag.Anime, p = i })">@i</a>
                </li>
            }
            @if (ViewBag.CurrentPage < ViewBag.PageCount)
            {
                <li class="page-item">
                    <a class="page-link" href="@Url.Action("Anime", new { anime = ViewBag.Anime, p = ViewBag.CurrentPage + 1 })" aria-label="Next">
                        <span aria-hidden="true">&raquo;</span>
                    </a>
                </li>
            }
        </ul>
    </nav>
</div>


